pipeline:
  build:
    image: ubuntu:xenial
    commands:
      - apt list --installed
      - apt install git
      - git clone https://github.com/eisoku9618/kuroiwa_demos.git
      - cd tex; make

services:
  postgres:
    image: postgres:9.4.5
    environment:
      - POSTGRES_USER=myapp
